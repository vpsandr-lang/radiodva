services:
  # Main web service: serves API, streaming, and static files
  - type: web
    name: radiodva
    runtime: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: "5050"
    healthCheckPath: /api/health
